apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
  namespace: {{ .Values.namespace }}
data:
  services-to-check: {{ .Values.servicesToCheck | default "ALL" | quote }}
  log-level: {{ .Values.logLevel | default "info" | quote }}
